pipeline {
  agent {
    docker {
      image 'node:16-alpine'
    }
  }

  stages {
    stage('Docker Auth and Test') {
      steps {
        script {
          withDockerRegistry([credentialsId: 'Dockerhub-credentials', url: 'https://index.docker.io/v1/']) {
            sh 'node --version'
          }
        }
      }
    }
  }
}
